Lints, commits, and  pushes a new command to an appropriately named branch.

Initialize a local git repo with your fork of tldr-pages set as origin,
run this script as shown below, then submit the pull request in github.

Usage:
$ new_command.sh {{command.md}}
